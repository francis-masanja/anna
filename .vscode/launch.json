{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "julia",
            "name": "Run main.jl",
            "request": "launch",
            "program": "${workspaceFolder}/src/main.jl",
            "stopOnEntry": false,
            "cwd": "${workspaceFolder}",
            "juliaEnv": "${command:activeJuliaEnvironment}"
        }
    ]
}
